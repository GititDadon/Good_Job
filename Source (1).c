#include<stdio.h>
#include<string.h>

void printf_employee_details(Employee w)// פרטי עובד
{
	int n;
	puts(w.first_name);
	puts(w.last_name);
	puts(w.last_name);
	puts(w.gender);
	printf("%s",w.id);
	printf("%d",w.age);
	puts(w.phone_number);
	printf("%d",w.password);
	puts(w.Email);
	puts(w.living_area);
	puts(w.Driving_License);
	puts(w.Education_level);
	puts(w.Employment_Catagory);
	printf("%d",w.policies);
	
	
}


	if (6 == n)
	{
		printf("Goodbye");
		return 0;
	}



	if (7 == n)
	{
		printf("Goodbye");
		return 0;
	}

